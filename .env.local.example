# OpenAuth Configuration
# Your OpenAuth issuer URL
OPENAUTH_ISSUER=https://auth.kajkowalski.nl
# Client ID for your app (can be any string that identifies your app)
OPENAUTH_CLIENT_ID=vetmed-tracker
# Client secret for OAuth 2.0 confidential client flows (REQUIRED)
OPENAUTH_CLIENT_SECRET=your-client-secret-here
# The public URL of your app (used for OAuth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database Configuration (Neon)
DATABASE_URL=postgresql://user:password@host/database

# Optional: Mock user ID for development when OpenAuth is not configured
# MOCK_USER_ID=dev-user-1

# Optional: Default timezone for new households (defaults to UTC if not set)
# DEFAULT_TIMEZONE=America/New_York

# Optional: Security Settings (Recommended for production)
# IMPORTANT: Uncomment these settings for production deployments
# COOKIE_SECURE=true              # Ensure cookies are only sent over HTTPS
# COOKIE_SAME_SITE=strict         # Strict same-site cookie policy for enhanced security
